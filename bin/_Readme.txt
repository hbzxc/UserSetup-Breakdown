Run the FirstTimeSetup.ps1 if it has not run before. It will populate some of the support files and install prereqs

It will save your credentals for use in the scripts using windows credental manager

 usernamme use this format -> hblazier@somecompany.com